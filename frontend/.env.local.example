# Leave unset to use the built-in mock API routes under src/app/api/*.
# Point this at the real FastAPI backend wrapper once it exists, e.g.:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=
